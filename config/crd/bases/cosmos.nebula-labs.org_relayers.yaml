---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: relayers.cosmos.nebula-labs.org
spec:
  group: cosmos.nebula-labs.org
  names:
    kind: Relayer
    listKind: RelayerList
    plural: relayers
    singular: relayer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Relayer is the Schema for the relayers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RelayerSpec defines the desired state of Relayer
            properties:
              foo:
                description: Foo is an example field of Relayer. Edit relayer_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: RelayerStatus defines the observed state of Relayer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
