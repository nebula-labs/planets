# permissions for end users to view relayers.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: relayer-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: planets
    app.kubernetes.io/part-of: planets
    app.kubernetes.io/managed-by: kustomize
  name: relayer-viewer-role
rules:
- apiGroups:
  - cosmos.nebula-labs.org
  resources:
  - relayers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - cosmos.nebula-labs.org
  resources:
  - relayers/status
  verbs:
  - get
